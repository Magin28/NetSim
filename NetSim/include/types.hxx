#include <functional>
using ElementID = int;

using TimeOffset = unsigned;
using Time = unsigned;

using Time = unsigned;

using TimeOffset = Time;

using ProbabilityGenerator =
    std::function<double()>;
